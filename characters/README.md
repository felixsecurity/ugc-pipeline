# Characters

Reusable character references for scripted Process B variants live here.

Each character gets its own folder:

```text
characters/<character_id>/
  character.json
  reference.png
  README.md
```

The current character library contains:

- `astrid`: blonde UGC presenter reference, sourced from `/srv/ugc_4k_blonde.png`.
